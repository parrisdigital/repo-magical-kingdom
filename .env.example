# Optional. Public repositories work without a token but are limited by GitHub's
# unauthenticated API quota. Use a zero-scope or public-repository-only token.
GITHUB_TOKEN=

# Public canonical origin used in metadata and sharing.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
